import CreateProjectForm from "@/features/projects/CreateProjectForm";

export const metadata = {
  title: "Add project | NawyTask",
};

export default function CreateProjectPage() {
  return <CreateProjectForm />;
}
